#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n = 0;
    
    vector<int> a(8);
    cin >> n;
    vector<int> b(n);
    for (int i = 0; i < n; i++)
    {
        cin >> b[i];
    }

    for (int i = 0; i < n;i++)
    {
        
    }
}